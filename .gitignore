# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# uv
# uv.lock 建议提交，不忽略

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# 测试与工具缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 环境与密钥
.env
.env.*

# macOS / 编辑器
.DS_Store
.settings/
.vscode/
.idea/
.continue/
.opencode/

# WorkBuddy 工作区数据（会话记忆、任务追踪等，不入库）
.workbuddy/

# 日志与临时文件
*.log
*.tmp

# LaTeX 编译产物
*.aux
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.synctex
*.xdv

# 编译出的二进制（无扩展名，按需保留）
graphics/test/cmath/pendulum
graphics/test/cmath/pendulum1

# 父仓不跟踪的目录（三个独立子仓 + 第三方源码）
#   ⚠️ 不加这几行时它们会一直显示为 ??；
#   一旦误跑 `git add -A`，会被当作 embedded repo 写出 gitlink 进索引，
#   而 AGENTS.md 第九节明确要求「无 .gitmodules、无 gitlink」。
#   三子仓改动一律在各自仓内提交，见 AGENTS.md 第七节。
learning-todo/
learning-viz/
learning-web/
graphics/third_party/
