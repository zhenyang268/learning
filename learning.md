# learning.md — 数学夯实计划（外部生成的细节版）

> **本文件定位**：这是数学侧的"亲手算一遍"清单（两部分：12 阶段经典问题路线 + 6 阶段几何物理读书路线），
> 与 `graphics/plan.md`（代码实现与验收里程碑）互补：
> - 凡 learning.md 中的计算**可以用代码验证**的，落到 plan.md 对应阶段的 demo 里做，双向受益；
> - learning.md 只管"手推验收"，不改变 AGENTS.md 的分层与硬规则。
>
> **与 graphics/plan.md 阶段的对照**（手推 ↔ 实现互相印证）：
>
> | learning.md | graphics/plan.md |
> |---|---|
> | 第二阶段（矩阵分析/无穷维矩阵） | 阶段 1–2（LU/特征值/SVD/expm） |
> | 第三阶段（ODE 与特殊函数）、4.3 辛积分器验证 | 阶段 3（积分器 + 单摆相图） |
> | 第七阶段（PDE 三大方程）、7.3 变分与 FEM | 阶段 4（FDM/CG/Poisson） |
> | 第九阶段（概率与蒙特卡洛） | 阶段 5（采样 + 光线追踪） |
> | 第八阶段（李群计算）、第六阶段（量子） | 阶段 2 验收 B4/B6 + 阶段 6（球谐/波包） |
> | 第一/四/五阶段（分析/变分/电磁） | 暂缓层，回读 notebook A/E 章时配套做 |

---

# 第一部分：夯实路线——通过经典问题覆盖全部数学

---

## 总原则

夯实不是"再读一遍"，而是**亲手算一遍**。每个模块给出：
- **必须独立完成的计算**（不看答案能推出来）
- **经典历史问题**（历史上推动理论发展的具体问题）
- **跨领域验证**（用 A 领域的方法解 B 领域的问题）

---

## 第一阶段：分析学的计算基础

### 1.1 勒贝格积分与收敛定理

**必须完成的计算：**

1. 证明 $f_n(x) = n \cdot \mathbf{1}_{(0, 1/n)}(x)$ 逐点收敛到 0，但 $\int f_n = 1$ 不收敛到 0。说明为什么 DCT 不适用（找不到控制函数）。

2. 计算 $\lim_{n\to\infty} \int_0^1 \frac{n x}{1 + n^2 x^2}\,dx$。先猜答案，再用 DCT 或 MCT 严格证明。

3. 证明 $\int_0^\infty e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2}$（用极坐标换元，涉及勒贝格积分的 Fubini 定理）。

4. 构造一个黎曼不可积但勒贝格可积的函数（狄利克雷函数），计算其勒贝格积分。

**经典问题：**

- **Vitali 集合**：构造一个不可测集。理解为什么需要选择公理，以及为什么不是所有集合都可测。
- **Cantor 集与 Cantor 函数（魔鬼阶梯）**：一个测度为零的集合上定义的连续函数，导数几乎处处为零但函数不是常数。理解"奇异"的含义。

### 1.2 $L^p$ 空间与泛函分析

**必须完成的计算：**

1. 证明 $L^2[0,1]$ 中 $\{1, \sqrt{2}\cos 2\pi nx, \sqrt{2}\sin 2\pi nx\}_{n=1}^\infty$ 是正交基。计算 $f(x) = x$ 的傅里叶系数，验证 Parseval 定理：$\int_0^1 x^2\,dx = \sum |c_n|^2$。

2. 在 $L^2[-1,1]$ 中，将 $f(x) = x^3$ 展开为 Legendre 多项式。验证正交性。

3. 证明柯西-施瓦茨不等式 $|\langle f, g\rangle| \leq \|f\|\|g\|$，并找出等号成立条件。

4. 计算积分算子 $(Tf)(x) = \int_0^1 K(x,y)f(y)\,dy$（$K$ 给定）的特征值和特征函数。具体取 $K(x,y) = \min(x,y)$（这是布朗运动的协方差核！）。

**经典问题：**

- **Hilbert 矩阵**：$H_{ij} = \frac{1}{i+j-1}$。证明它是 $L^2[0,1]$ 上某个积分算子的矩阵表示。计算其条件数（无穷大！），理解为什么是病态的。
- **Sturm-Liouville 问题的完备性**：对 $-y'' = \lambda y$，$y(0) = y(\pi) = 0$，证明特征函数 $\{\sin nx\}$ 构成 $L^2[0,\pi]$ 的完备正交基。

### 1.3 分布与格林函数

**必须完成的计算：**

1. 验证 $\delta'(x)$ 的作用：$\langle \delta', \varphi \rangle = -\varphi'(0)$。

2. 计算 $\mathcal{F}[\delta] = 1$，$\mathcal{F}[1] = 2\pi\delta$（傅里叶变换的分布意义）。

3. 求 $-u'' = \delta(x - \xi)$，$u(0) = u(1) = 0$ 的解（即格林函数），验证 $G$ 在 $x = \xi$ 处连续但导数跳跃。

4. 求三维拉普拉斯方程的基本解：证明 $\nabla^2 \frac{1}{4\pi r} = -\delta(\mathbf{x})$（用散度定理）。

**经典问题：**

- **Dirac 的 $\delta$ 函数**：历史上 Dirac 在量子力学中引入 $\delta$ 函数，但当时没有严格数学基础。直到 Schwartz 在 1940 年代建立分布理论才严格化。尝试用"测试函数"的语言重新表述量子力学中的 $\langle x | x' \rangle = \delta(x - x')$。

---

## 第二阶段：线性代数与矩阵分析

### 2.1 矩阵分解

**必须完成的计算：**

1. 对矩阵 $A = \begin{pmatrix} 1 & 2 \\ 3 & 4 \end{pmatrix}$ 做 LU、QR、SVD、特征分解。验证每种分解的正确性。

2. 对协方差矩阵 $C = \frac{1}{n}\sum \mathbf{x}_i \mathbf{x}_i^T$ 做特征分解，理解 PCA。用具体数据（如 2D 高斯分布的采样点）验证主成分方向。

3. 证明：$A$ 是正定矩阵 $\iff$ 所有特征值 > 0 $\iff$ 存在 Cholesky 分解 $A = LL^T$ $\iff$ 所有顺序主子式 > 0。

4. 计算矩阵指数 $e^{At}$，其中 $A = \begin{pmatrix} 0 & 1 \\ -1 & 0 \end{pmatrix}$。验证 $e^{At}$ 是旋转矩阵。

**经典问题：**

- **Google 的 PageRank**：本质是求一个随机矩阵的主特征向量（Perron-Frobenius 定理）。构造一个 5 个网页的小例子，计算 PageRank。
- **振动模式分析**：$M\ddot{x} + Kx = 0$（$M$ 质量矩阵，$K$ 刚度矩阵）。化为广义特征值问题 $K\phi = \omega^2 M\phi$。对 3 个质点-弹簧系统计算所有振动频率和模式。

### 2.2 无穷维矩阵与算子

**必须完成的计算：**

1. 在基 $\{1, x, x^2, x^3, \ldots\}$ 下，写出微分算子 $D = d/dx$ 的矩阵表示（无穷维）。计算 $D^2$ 的矩阵。验证 $e^{tD}$ 的矩阵给出 Taylor 公式。

2. 在基 $\{\sin nx\}_{n=1}^\infty$ 下，写出 $-d^2/dx^2$（$[0,\pi]$，Dirichlet 边界）的矩阵。验证它是对角的，特征值为 $n^2$。

3. 在基 $\{e^{inx}\}_{n\in\mathbb{Z}}$ 下，写出 $-d^2/dx^2$（$[0,2\pi]$，周期边界）的矩阵。

4. 取 $V(x) = \cos x$，在傅里叶基下写出 $-d^2/dx^2 + \cos x$ 的矩阵（Mathieu 方程的算子）。截断为 $5\times 5$，数值计算特征值。

**经典问题：**

- **Mathieu 方程**：$y'' + (a - 2q\cos 2x)y = 0$。这是椭圆鼓膜的振动方程，也是周期势中量子力学（能带理论）的模型。特征值 $a(q)$ 形成"能带"结构。
- **氢原子的矩阵力学**：Heisenberg 1925 年用无穷维矩阵（而非波函数）表述量子力学。尝试在 $|n\rangle$ 基下写出氢原子哈密顿量的矩阵。

---

## 第三阶段：常微分方程与特殊函数

### 3.1 经典 ODE 的完整求解

**必须完成的计算：**

1. **Airy 方程** $y'' - xy = 0$：用幂级数法求两个线性无关解。确定收敛半径（无穷大）。画出 $Ai(x)$ 和 $Bi(x)$ 的大致形状。

2. **Bessel 方程** $x^2y'' + xy' + (x^2 - \nu^2)y = 0$：用 Frobenius 方法求 $J_\nu(x)$。验证 $J_0(x)$ 满足方程。计算 $\int_0^1 x J_0(\alpha x)^2\,dx$（$\alpha$ 是 $J_0$ 的零点）。

3. **Hermite 方程** $y'' - 2xy' + 2ny = 0$：用幂级数法求解，验证 $H_0 = 1$，$H_1 = 2x$，$H_2 = 4x^2 - 2$。验证正交性 $\int_{-\infty}^\infty H_m H_n e^{-x^2}\,dx = 0$（$m \neq n$）。

4. **Legendre 方程**：验证 $P_0 = 1$，$P_1 = x$，$P_2 = \frac{1}{2}(3x^2 - 1)$。用 Rodrigues 公式推导。验证 $\int_{-1}^1 P_m P_n\,dx = \frac{2}{2n+1}\delta_{mn}$。

**经典问题：**

- **三体问题与混沌**：限制性三体问题的运动方程是耦合的非线性 ODE。证明不存在一般的解析解（Poincaré）。理解为什么需要数值方法和定性理论。
- **Van der Pol 振荡器**：$\ddot{x} - \mu(1-x^2)\dot{x} + x = 0$。对 $\mu = 0$ 是简谐振动；$\mu > 0$ 有极限环。用相平面分析定性行为。

### 3.2 格林函数与边值问题

**必须完成的计算：**

1. 求 $-y'' = f(x)$，$y(0) = y(1) = 0$ 的格林函数。用 $f(x) = x(1-x)$ 验证。

2. 求 $-y'' + y = f(x)$，$y(0) = y(1) = 0$ 的格林函数（与上题比较，多了 $+y$ 项）。

3. 用特征函数展开法重新求格林函数：$G(x,\xi) = \sum_{n=1}^\infty \frac{\sin n\pi x \sin n\pi \xi}{n^2\pi^2}$。验证与直接法结果一致。

4. 求二维拉普拉斯方程在圆盘 $r < 1$ 上的格林函数（用镜像法或分离变量）。

**经典问题：**

- **Dirichlet 问题**：给定圆盘边界上的温度分布，求内部温度。这就是拉普拉斯方程的边值问题。Poisson 积分公式给出解析解。
- **Sturm 的比较定理**：若 $q_1(x) \leq q_2(x)$，则 $-y'' + q_2 y = \lambda y$ 的特征值 $\geq$ $-y'' + q_1 y = \lambda y$ 的特征值。物理意义：势阱越深，能级越低。

---

## 第四阶段：变分法与经典力学

### 4.1 变分法的核心计算

**必须完成的计算：**

1. **最速降线问题（Brachistochrone）**：从 $(0,0)$ 到 $(a,b)$，在重力下最快到达的曲线。
   - 写出泛函 $T = \int_0^a \sqrt{\frac{1+y'^2}{2gy}}\,dx$
   - 用欧拉-拉格朗日方程推导
   - 验证解是摆线（旋轮线）

2. **测地线问题**：在球面上，两点之间的最短路径是大圆。
   - 写出球面上的弧长泛函
   - 用欧拉-拉格朗日方程推导
   - 验证大圆满足方程

3. **悬链线问题（Catenary）**：两端固定的链条在重力下的形状。
   - 泛函：$\int y\sqrt{1+y'^2}\,dx$（势能最小）
   - 解：$y = a\cosh(x/a)$

4. **等周问题**：给定周长，面积最大的封闭曲线是圆。
   - 用拉格朗日乘子处理约束
   - 验证圆满足欧拉-拉格朗日方程

**经典问题：**

- **Fermat 原理与折射定律**：光走时间最短的路径。从变分法推导 Snell 定律 $n_1\sin\theta_1 = n_2\sin\theta_2$。
- **最小曲面**：肥皂膜的形状。泛函 $\int\int \sqrt{1 + u_x^2 + u_y^2}\,dx\,dy$。欧拉-拉格朗日方程是非线性 PDE（最小曲面方程）。

### 4.2 拉格朗日力学的完整练习

**必须完成的计算：**

1. **单摆**：$L = \frac{1}{2}ml^2\dot{\theta}^2 - mgl(1-\cos\theta)$
   - 推导运动方程：$\ddot{\theta} + \frac{g}{l}\sin\theta = 0$
   - 小角度近似：$\ddot{\theta} + \frac{g}{l}\theta = 0$
   - 求周期（精确解用椭圆积分）

2. **双摆**：两个质点，两根杆。
   - 写出拉格朗日量（2 个自由度）
   - 推导耦合的运动方程
   - 分析小振动模式（特征值问题！）

3. **中心力问题**：$L = \frac{1}{2}m(\dot{r}^2 + r^2\dot{\theta}^2) - V(r)$
   - 由 $\theta$ 的循环坐标性 → 角动量守恒
   - 约化为有效势问题：$V_{\text{eff}}(r) = V(r) + \frac{l^2}{2mr^2}$
   - 对 $V = -k/r$（引力），推导轨道方程 $r = \frac{p}{1+e\cos\theta}$（圆锥曲线）

4. **Noether 定理的验证**：
   - 自由粒子 $L = \frac{1}{2}m\dot{x}^2$：平移不变 → 动量守恒
   - 中心力：旋转不变 → 角动量守恒
   - 不含时：时间平移不变 → 能量守恒

### 4.3 哈密顿力学

**必须完成的计算：**

1. **谐振子的相空间**：$H = \frac{p^2}{2m} + \frac{1}{2}m\omega^2 q^2$
   - 哈密顿方程：$\dot{q} = p/m$，$\dot{p} = -m\omega^2 q$
   - 相轨迹是椭圆
   - 验证泊松括号 $\{q, H\} = \dot{q}$，$\{p, H\} = \dot{p}$
   - 验证 $\{q, p\} = 1$

2. **正则变换**：找生成函数 $F_2(q, P)$ 使得谐振子变为 $H' = P$（即 $P$ 是守恒量）。
   - 这就是作用量-角变量 $(J, \theta)$ 变换

3. **Hamilton-Jacobi 方程**：对自由粒子 $H = p^2/2m$，解 $S(q, t)$。
   - $\frac{\partial S}{\partial t} + \frac{1}{2m}\left(\frac{\partial S}{\partial q}\right)^2 = 0$
   - 验证 $S = \frac{m(q-q_0)^2}{2t}$ 是解

4. **泊松括号与量子对易子**：验证经典泊松括号 $\{q, p\} = 1$ 对应量子对易子 $[\hat{q}, \hat{p}] = i\hbar$。对 $\{q^2, p\}$ 和 $[\hat{q}^2, \hat{p}]$ 做同样的验证。

**经典问题：**

- **KAM 定理**：近可积哈密顿系统的不变环面在微扰下大部分存活。这是经典力学中"混沌"与"规则"共存的原因。
- **辛积分器的验证**：对谐振子，分别用 Euler 法、RK4、Verlet 积分 1000 步，画出能量随时间的变化。观察 Euler 能量漂移、RK4 缓慢漂移、Verlet 有界振荡。

---

## 第五阶段：电磁学与对称性

### 5.1 Maxwell 方程的对称性

**必须完成的计算：**

1. **从库仑定律推导高斯定律**：$\oint \mathbf{E}\cdot d\mathbf{A} = Q/\varepsilon_0$。用散度定理得到 $\nabla\cdot\mathbf{E} = \rho/\varepsilon_0$。

2. **从 Biot-Savart 定律推导安培定律**：$\oint \mathbf{B}\cdot d\mathbf{l} = \mu_0 I$。

3. **验证电磁波的波动方程**：从 Maxwell 方程推导 $\nabla^2 \mathbf{E} - \frac{1}{c^2}\frac{\partial^2 \mathbf{E}}{\partial t^2} = 0$，确认 $c = 1/\sqrt{\mu_0\varepsilon_0}$。

4. **规范不变性验证**：$A_\mu \to A_\mu + \partial_\mu \lambda$，验证 $F_{\mu\nu} = \partial_\mu A_\nu - \partial_\nu A_\mu$ 不变。

5. **Lorentz 协变性**：将 Maxwell 方程写成 $\partial_\nu F^{\mu\nu} = \mu_0 J^\mu$ 和 $\partial_{[\lambda} F_{\mu\nu]} = 0$。验证在 Lorentz 变换下形式不变。

**经典问题：**

- **Aharonov-Bohm 效应**：在 $\mathbf{B} = 0$ 的区域，矢势 $\mathbf{A} \neq 0$ 仍然影响量子相位。这说明 $\mathbf{A}$（而非 $\mathbf{B}$）是基本的物理量。拓扑上，这对应 $U(1)$ 丛的非平凡性（$\pi_1(U(1)) = \mathbb{Z}$）。
- **磁单极与拓扑**：如果存在磁单极，则 $\nabla\cdot\mathbf{B} \neq 0$，矢势不能全局定义（需要两个 patch）。Dirac 量子化条件 $eg = n\hbar/2$ 来自 $U(1)$ 丛的拓扑（第一 Chern 类）。

### 5.2 电磁学中的变分原理

**必须完成的计算：**

1. 从作用量 $S = \int \left(-\frac{1}{4}F_{\mu\nu}F^{\mu\nu} - J^\mu A_\mu\right)d^4x$ 推导 Maxwell 方程。

2. 验证能量守恒（Poynting 定理）：$\frac{\partial u}{\partial t} + \nabla\cdot\mathbf{S} = -\mathbf{J}\cdot\mathbf{E}$，其中 $u = \frac{1}{2}(\varepsilon_0 E^2 + B^2/\mu_0)$，$\mathbf{S} = \mathbf{E}\times\mathbf{B}/\mu_0$。

3. 验证动量守恒（Maxwell 应力张量）。

### 5.3 对称性与守恒量的完整对应

**必须完成的计算：**

| 对称性 | 变换 | 守恒量 | 验证 |
|--------|------|--------|------|
| 时间平移 | $t \to t + \epsilon$ | 能量 | 对谐振子验证 |
| 空间平移 | $\mathbf{x} \to \mathbf{x} + \boldsymbol{\epsilon}$ | 动量 | 对自由粒子验证 |
| 空间旋转 | $\mathbf{x} \to R\mathbf{x}$ | 角动量 | 对中心力验证 |
| 规范变换 | $A_\mu \to A_\mu + \partial_\mu\lambda$ | 电荷 | 验证 $\partial_\mu J^\mu = 0$ |
| Lorentz boost | $x^\mu \to \Lambda^\mu_\nu x^\nu$ | 质心运动 | 验证 |

---

## 第六阶段：量子力学的计算

### 6.1 基本量子系统

**必须完成的计算：**

1. **无限深势阱**：$V = 0$（$0 < x < L$），$V = \infty$（其他）
   - 解薛定谔方程，得 $E_n = \frac{n^2\pi^2\hbar^2}{2mL^2}$，$\psi_n = \sqrt{2/L}\sin(n\pi x/L)$
   - 验证正交性和完备性
   - 计算 $\langle x \rangle$，$\langle p \rangle$，$\Delta x$，$\Delta p$，验证不确定性原理

2. **谐振子**：$H = \frac{p^2}{2m} + \frac{1}{2}m\omega^2 x^2$
   - 用升降算子 $a^\dagger, a$ 求解
   - $[a, a^\dagger] = 1$，$H = \hbar\omega(a^\dagger a + 1/2)$
   - $E_n = \hbar\omega(n + 1/2)$
   - 验证基态 $\psi_0 \propto e^{-m\omega x^2/2\hbar}$ 满足不确定性原理的等号

3. **氢原子**：
   - 分离变量（球坐标）：$R(r)Y_l^m(\theta,\phi)$
   - 角度部分 → 球谐函数
   - 径向部分 → 关联 Laguerre 多项式
   - $E_n = -\frac{13.6\text{ eV}}{n^2}$
   - 验证 $SO(4)$ 隐藏对称性（Runge-Lenz 向量）

4. **自旋 1/2**：
   - Pauli 矩阵 $\sigma_x, \sigma_y, \sigma_z$
   - 验证 $[\sigma_i, \sigma_j] = 2i\varepsilon_{ijk}\sigma_k$
   - Stern-Gerlach 实验的矩阵计算
   - 自旋在 $z$ 方向测量后，再在 $x$ 方向测量的概率

### 6.2 路径积分的计算

**必须完成的计算：**

1. **自由粒子传播子**：直接计算高斯路径积分，验证：
$$K_{\text{free}} = \sqrt{\frac{m}{2\pi i\hbar T}} e^{im(x_f-x_i)^2/2\hbar T}$$

2. **谐振子传播子**：验证经典作用量代入后给出正确结果。

3. **稳相近似**：对 $K = \int \mathcal{D}x\, e^{iS[x]/\hbar}$，在 $\hbar \to 0$ 极限下，证明主导贡献来自 $\delta S = 0$ 的路径（经典路径）。

4. **Wick 转动**：$t \to -i\tau$，将 $e^{iS/\hbar}$ 变为 $e^{-S_E/\hbar}$。验证谐振子的欧几里得传播子与统计力学的配分函数一致。

**经典问题：**

- **双缝干涉的路径积分解释**：粒子通过两条缝的振幅相加（而非概率相加），产生干涉。
- **瞬子（Instanton）**：双势阱中的隧穿效应。在欧几里得路径积分中，瞬子是连接两个势阱底的经典解。

### 6.3 角动量与表示论

**必须完成的计算：**

1. 验证 $[L_x, L_y] = i\hbar L_z$（用 $L_i = \varepsilon_{ijk}x_j p_k$ 和 $[x_i, p_j] = i\hbar\delta_{ij}$）。

2. 用升降算子 $L_\pm = L_x \pm iL_y$ 推导 $l$ 和 $m$ 的取值范围。

3. 计算 $Y_1^0 = \sqrt{3/4\pi}\cos\theta$，$Y_1^{\pm 1} = \mp\sqrt{3/8\pi}\sin\theta\, e^{\pm i\phi}$。验证它们是 $L^2$ 和 $L_z$ 的本征函数。

4. **Clebsch-Gordan 系数**：两个自旋 $1/2$ 粒子的耦合。$|1,1\rangle = |\uparrow\uparrow\rangle$，$|1,0\rangle = \frac{1}{\sqrt{2}}(|\uparrow\downarrow\rangle + |\downarrow\uparrow\rangle)$，$|0,0\rangle = \frac{1}{\sqrt{2}}(|\uparrow\downarrow\rangle - |\downarrow\uparrow\rangle)$。

---

## 第七阶段：PDE 与数学物理

### 7.1 三大方程的完整求解

**必须完成的计算：**

1. **一维波动方程**：$u_{tt} = c^2 u_{xx}$，$u(0,t) = u(L,t) = 0$，$u(x,0) = f(x)$，$u_t(x,0) = g(x)$
   - 分离变量 → 傅里叶正弦级数
   - d'Alembert 公式
   - 验证能量守恒：$E = \frac{1}{2}\int_0^L (u_t^2 + c^2 u_x^2)\,dx$ 不随时间变化

2. **一维热方程**：$u_t = ku_{xx}$，$u(0,t) = u(L,t) = 0$，$u(x,0) = f(x)$
   - 分离变量 → 傅里叶正弦级数，系数含 $e^{-k(n\pi/L)^2 t}$
   - 验证：$t \to \infty$ 时 $u \to 0$（热平衡）
   - 验证极值原理

3. **圆盘上的拉普拉斯方程**：$\nabla^2 u = 0$，$r < 1$，$u(1,\theta) = f(\theta)$
   - 极坐标分离变量
   - 解：$u(r,\theta) = \frac{a_0}{2} + \sum_{n=1}^\infty r^n(a_n\cos n\theta + b_n\sin n\theta)$
   - Poisson 积分公式：$u(r,\theta) = \frac{1}{2\pi}\int_0^{2\pi} \frac{1-r^2}{1-2r\cos(\theta-\phi)+r^2}f(\phi)\,d\phi$

4. **三维波动方程的 Kirchhoff 公式**：验证 Huygens 原理（三维波有锐利波前）。

### 7.2 积分变换的应用

**必须完成的计算：**

1. 用 Fourier 变换解 $\mathbb{R}$ 上的热方程，验证解是热核与初始数据的卷积。

2. 用 Laplace 变换解 $y'' + 2y' + 5y = e^{-t}\sin t$，$y(0) = 0$，$y'(0) = 0$。

3. 用 Laplace 变换解半直线上的热方程：$u_t = ku_{xx}$，$x > 0$，$u(0,t) = 1$，$u(x,0) = 0$。解涉及误差函数 $\text{erf}$。

4. 验证卷积定理：$\mathcal{L}\{f * g\} = \mathcal{L}\{f\}\cdot\mathcal{L}\{g\}$。

### 7.3 变分法与 PDE

**必须完成的计算：**

1. 证明 $-\nabla^2 u = f$（$u|_{\partial\Omega} = 0$）等价于最小化 $J[u] = \int_\Omega \frac{1}{2}|\nabla u|^2 - fu\,d\Omega$。

2. 推导弱形式，验证对称性：$a(u,v) = a(v,u)$。

3. 对一维情况 $-u'' = 1$，$u(0) = u(1) = 0$：
   - 精确解：$u = x(1-x)/2$
   - 用线性有限元（2 个单元）求近似解
   - 比较误差

**经典问题：**

- **Plateau 问题**：给定空间中的闭合曲线，找以它为边界的最小面积曲面。这是变分法的经典问题，解是最小曲面。
- **特征值问题的变分刻画**：$\lambda_1 = \min_{u \neq 0} \frac{\int|\nabla u|^2}{\int u^2}$（Rayleigh 商）。用这个公式估计圆形鼓膜的第一振动频率。

---

## 第八阶段：李群与对称性的计算

### 8.1 具体的李群计算

**必须完成的计算：**

1. **$SO(3)$ 的指数映射**：给定 $\omega = (0, 0, \theta)$，计算 $e^{[\omega]_\times}$，验证是绕 $z$ 轴旋转 $\theta$ 的矩阵。

2. **BCH 公式验证**：取 $X = \epsilon J_1$，$Y = \epsilon J_2$（$\mathfrak{so}(3)$ 的基），计算 $e^X e^Y$ 和 $e^{X+Y+\frac{1}{2}[X,Y]}$，比较到 $O(\epsilon^2)$。

3. **$SU(2) \to SO(3)$ 的覆盖映射**：给定四元数 $q = \cos\frac{\theta}{2} + \sin\frac{\theta}{2}\hat{n}$，计算对应的旋转矩阵 $R$。验证 $q$ 和 $-q$ 给出同一个 $R$。

4. **$\mathfrak{so}(3)$ 的伴随表示**：计算 $\text{ad}_{J_3}$ 的矩阵（在基 $\{J_1, J_2, J_3\}$ 下），验证它是 $3\times 3$ 反对称矩阵。

### 8.2 对称性解 ODE

**必须完成的计算：**

1. 对 $y' = y/x + x^2/y$（齐次方程），找对称性 $\mathbf{v} = x\partial_x + y\partial_y$，用不变量 $r = y/x$ 降阶。

2. 对 $y'' = f(y)$（缺 $x$），用平移对称性 $\partial_x$ 降阶。

3. 对 Euler 方程 $x^2y'' + axy' + by = 0$，用尺度对称性 $x\partial_x$ 化为常系数。

### 8.3 对称性解 PDE

**必须完成的计算：**

1. 热方程 $u_t = u_{xx}$ 的尺度对称性：$(x, t, u) \to (\lambda x, \lambda^2 t, u)$。用不变量 $\xi = x/\sqrt{t}$ 化为 ODE，得到自相似解（热核）。

2. 波动方程 $u_{tt} = c^2 u_{xx}$ 的平移对称性：$(x, t) \to (x - ct, t)$。用不变量 $\xi = x - ct$ 得到行波解。

---

## 第九阶段：概率与蒙特卡洛

### 9.1 概率的计算

**必须完成的计算：**

1. 证明正态分布 $N(0,1)$ 的特征函数是 $\hat{f}(t) = e^{-t^2/2}$。

2. 用特征函数证明中心极限定理（对独立同分布随机变量和）。

3. 计算二维正态分布 $(X, Y)$ 在极坐标下的联合密度，验证 $R$ 和 $\Theta$ 独立（当 $\rho = 0$ 时）。

4. 推导指数分布的无记忆性：$P(X > s + t \mid X > s) = P(X > t)$。

### 9.2 蒙特卡洛方法

**必须完成的计算：**

1. 用蒙特卡洛估计 $\pi$：在 $[0,1]^2$ 中均匀采样，计算落在单位圆内的比例。

2. 用重要性采样估计 $\int_0^\infty e^{-x^2}\,dx$：
   - 朴素采样（均匀截断）
   - 用 $p(x) \propto e^{-x}$ 做重要性采样
   - 比较方差

3. 实现简单的路径追踪器（1D 版本）：
   - 一维场景：两个漫反射墙，中间一个光源
   - 渲染方程：$L = L_e + \rho \int L\,d\omega$（简化为 $L = L_e + \rho L$）
   - 用 Neumann 级数和蒙特卡洛分别求解

4. 验证 MIS 的无偏性：对两个采样策略 $p_1, p_2$，验证 $\hat{I}_{MIS}$ 的期望等于真值。

---

## 第十阶段：数值方法的实现

### 10.1 ODE 求解器

**必须完成的计算/实现：**

1. 实现 Euler、RK2、RK4，对 $\dot{y} = -y$（$y(0) = 1$）比较精度和稳定性。

2. 实现 Verlet 积分，对谐振子跑 10000 步，画能量随时间的变化。与 RK4 比较。

3. 实现自适应步长 RK45（Dormand-Prince），对刚性问题 $\dot{y} = -1000y + 3000 - 2000e^{-t}$ 测试。

### 10.2 PDE 求解器

**必须完成的计算/实现：**

1. 用有限差分（显式）解一维热方程，验证稳定性条件 $r \leq 1/2$。

2. 用有限差分（隐式/Crank-Nicolson）解一维热方程，验证无条件稳定。

3. 用有限元（线性元）解 $-u'' = 1$，$u(0) = u(1) = 0$。组装刚度矩阵，求解，与精确解比较。

4. 用 FFT 解周期边界的一维泊松方程 $-u'' = f$。

### 10.3 流体模拟

**必须完成的计算/实现：**

1. 实现 2D Stable Fluids（Jos Stam）：
   - 半拉格朗日平流
   - 压力投影（用 FFT 或共轭梯度解泊松方程）
   - 验证无散度（$\nabla\cdot\mathbf{u} \approx 0$）

2. 验证能量衰减：$\frac{d}{dt}\int|\mathbf{u}|^2 \leq 0$（无外力时动能只减不增）。

---

## 第十一阶段：跨领域综合问题

### 11.1 量子力学 ↔ 渲染

**问题：** 路径追踪与费曼路径积分的数学结构对比。

**具体任务：**
1. 写出渲染方程的 Neumann 级数：$L = \sum_{k=0}^\infty \mathcal{T}^k L_e$
2. 写出费曼路径积分的微扰展开：$K = \sum_{k=0}^\infty K^{(k)}$
3. 对比：
   - 渲染的"弹射次数" ↔ 量子的"相互作用阶数"
   - 渲染的"几何衰减 $1/r^2$" ↔ 量子的"传播子"
   - 渲染的"BRDF" ↔ 量子的"顶点因子"
4. 关键区别：渲染是正实数（概率/能量），量子是复数（振幅，有干涉）

### 11.2 谱理论 ↔ 几何处理

**问题：** 网格上的拉普拉斯算子的谱。

**具体任务：**
1. 构造一个简单网格（如正四面体或正方形网格）
2. 组装余切拉普拉斯矩阵 $L$
3. 计算特征值和特征向量
4. 验证：
   - $\lambda_0 = 0$（常数函数）
   - $\lambda_1 > 0$（第一个非平凡模式）
   - 特征向量的零点个数随特征值增大而增加（类似 Sturm 定理）
5. 用前几个特征向量做"形状分割"

### 11.3 变分法 ↔ 有限元

**问题：** 从连续变分到离散有限元。

**具体任务：**
1. 连续问题：最小化 $\int_0^1 \frac{1}{2}(u')^2 - u\,dx$，$u(0) = u(1) = 0$
2. 推导欧拉-拉格朗日方程：$-u'' = 1$
3. 精确解：$u = x(1-x)/2$
4. 用 2 个线性有限元离散，组装 $2\times 2$ 系统，求解
5. 用 4 个线性有限元离散，组装 $4\times 4$ 系统，求解
6. 比较误差，验证收敛阶 $O(h^2)$

### 11.4 李群 ↔ 微分方程 ↔ 物理

**问题：** 从对称性到守恒量到量子化。

**具体任务：**
1. 经典谐振子有 $U(1)$ 对称性（相空间旋转）→ 能量守恒
2. 量子化后，$U(1)$ 的不可约表示是 $e^{in\theta}$（$n \in \mathbb{Z}$）→ 能量量子化 $E_n = \hbar\omega(n+1/2)$
3. 三维各向同性谐振子有 $SU(3)$ 对称性 → 能级简并度 $\frac{(n+1)(n+2)}{2}$
4. 氢原子有 $SO(4)$ 对称性 → 能级简并度 $n^2$

### 11.5 拓扑 ↔ 物理 ↔ CG

**问题：** 拓扑不变量在各领域的体现。

**具体任务：**
1. 计算球面 $S^2$ 的欧拉示性数 $\chi = 2$（用三角剖分：$V - E + F = 2$）
2. 计算环面 $T^2$ 的 $\chi = 0$
3. 验证 Gauss-Bonnet：$\int_{S^2} K\,dA = 4\pi = 2\pi\chi$
4. 在网格上验证离散 Gauss-Bonnet：$\sum_i \left(2\pi - \sum_j \theta_{ij}\right) = 2\pi\chi$
5. 讨论：为什么球面上的向量场必有零点（Hairy Ball），而环面上可以没有

---

## 第十二阶段：历史经典问题清单

按时间顺序，每个问题都涉及你学过的多个领域：

| 年代 | 问题 | 涉及知识 | 关键人物 |
|------|------|----------|----------|
| 1696 | 最速降线 | 变分法、ODE | Bernoulli、Newton |
| 1744 | 弹性曲线（Euler） | 变分法、椭圆积分 | Euler |
| 1788 | 分析力学 | 拉格朗日量、约束 | Lagrange |
| 1822 | 热传导理论 | 热方程、傅里叶级数 | Fourier |
| 1830s | 势论 | 拉普拉斯方程、格林函数 | Gauss、Green |
| 1843 | 四元数 | 非交换代数、旋转 | Hamilton |
| 1865 | Maxwell 方程 | 向量分析、对称性 | Maxwell |
| 1870s | 李群理论 | 连续对称性、微分方程 | Lie |
| 1890s | 三体问题 | 混沌、定性理论 | Poincaré |
| 1900 | 黑体辐射 | 量子化、统计力学 | Planck |
| 1905 | 狭义相对论 | Lorentz 群、闵可夫斯基时空 | Einstein |
| 1915 | 广义相对论 | 黎曼几何、变分法 | Einstein |
| 1925 | 矩阵力学 | 无穷维矩阵、非交换代数 | Heisenberg |
| 1926 | 波动力学 | 薛定谔方程、希尔伯特空间 | Schrödinger |
| 1928 | Dirac 方程 | 旋量、Clifford 代数 | Dirac |
| 1930s | 规范场论 | 纤维丛、联络 | Weyl、Yang、Mills |
| 1940s | 路径积分 | 泛函积分、作用量 | Feynman |
| 1948 | 信息论 | 熵、概率 | Shannon |
| 1954 | Yang-Mills 理论 | 非阿贝尔规范场 | Yang、Mills |
| 1960s | 标准模型 | $SU(3)\times SU(2)\times U(1)$ | Weinberg、Salam |
| 1968 | 渲染方程 | 积分方程、蒙特卡洛 | 后来由 Kajiya 1986 形式化 |
| 1970s | 瞬子 | 拓扑、规范场 | Belavin、Polyakov |
| 1984 | 弦论 | 共形场论、模空间 | Green、Schwarz |
| 1999 | Stable Fluids | NS 方程、半拉格朗日 | Stam |

---

## 执行建议

**每周安排（假设全职学习）：**

| 周 | 内容 | 产出 |
|----|------|------|
| 1-2 | 第一阶段（分析学计算） | 完成所有积分和收敛定理练习 |
| 3-4 | 第二阶段（矩阵分析） | 完成所有分解和无穷维矩阵计算 |
| 5-6 | 第三阶段（ODE 与特殊函数） | 完整推导 4 个特殊函数方程 |
| 7-8 | 第四阶段（变分法与力学） | 完成最速降线、双摆、Noether 验证 |
| 9-10 | 第五阶段（电磁与对称性） | 完成 Maxwell 的 4 种表述 |
| 11-12 | 第六阶段（量子力学） | 完整求解 4 个量子系统 |
| 13-14 | 第七阶段（PDE） | 完整求解三大方程 |
| 15-16 | 第八阶段（李群计算） | 完成所有指数映射和对称性降阶 |
| 17-18 | 第九阶段（概率与蒙特卡洛） | 实现简单路径追踪器 |
| 19-20 | 第十阶段（数值实现） | 实现流体模拟和有限元 |
| 21-24 | 第十一阶段（跨领域综合） | 完成 5 个综合问题 |

**核心原则：每个计算必须亲手推一遍，不能只"看懂"。** 看懂和能推出来之间隔着巨大的鸿沟。

---
---

# 第二部分：总览——你要攀登的四座山（几何物理读书路线）

你现在的知识版图可以概括为四根支柱，它们最终会汇合成一个东西：

- **支柱一：微分几何**（流形、切丛、微分形式）——你已经入门
- **支柱二：李群李代数**（对称性的语言）——你已经入门
- **支柱三：分析力学**（拉格朗日、哈密顿、辛几何）——需要系统学
- **支柱四：场论**（电磁、规范场、广义相对论）——需要系统学

$$\text{四者汇合} \longrightarrow \text{几何力学 + 规范理论 + 诺特定理}$$

下面给出完整路线，分六个阶段，每阶段给书、给练法、给检验标准。

## 阶段零：数学地基补强（1-2 个月，可并行）

你后面所有东西都依赖这三样，先确认它们扎实：

### 0.1 线性代数（真正学一遍，不是会算矩阵）

- 书：《Linear Algebra Done Right》(Axler) 或丘维声《高等代数》
- 重点：对偶空间、外代数（$\bigwedge^k V$）、二次型的惯性定律
- 检验标准：能不看书写出霍奇对偶在 $n$ 维带度量空间上的定义，并证明 $\star\star\omega = (-1)^{k(n-k)}\text{sign}(g)\,\omega$

### 0.2 多元微积分 + 张量运算

- 书：《Div, Grad, Curl, and All That》(Schey) —— 薄，一周读完
- 重点：向量分析恒等式全部用微分形式重写一遍（你已经会了一半）
- 检验标准：能证明 $d^2 = 0$ 蕴含 $\nabla \times \nabla f = 0$ 和 $\nabla \cdot (\nabla \times \vec{A}) = 0$

### 0.3 常微分方程（流的存在性）

- 书：Arnold《Ordinary Differential Equations》第 1-2 章，或任何 ODE 教材的存在唯一性定理部分
- 重点：流（flow）、相空间、向量场生成流的图像
- 检验标准：能说清楚"完备向量场"和"李群作用"的关系

## 阶段一：微分几何系统学（2-3 个月）★核心

你现在是"用过"微分形式，但还缺流形的严格定义、联络、曲率、de Rham 上同调。

### 1.1 主线教材（二选一）

| 书 | 风格 | 适合你如果 |
|---|---|---|
| 《Introduction to Smooth Manifolds》(John Lee) | 极其详细，定义严谨，习题丰富 | 想要无死角的地基 |
| 《Geometry, Topology and Physics》(Nakahara) | 物理导向，直接连到规范场 | 想快速通往场论 |

建议：以 Lee 为主线，前 11 章（流形、切空间、微分形式、Stokes、de Rham）精读，每章习题做一半以上。

### 1.2 必须掌握的内容清单

- [ ] 流形的定义、图册、光滑映射
- [ ] 切丛、余切丛、向量场、李导数 $\mathcal{L}_X$
- [ ] 微分形式、外微分、Stokes 定理（你已入门，现在要严格证明）
- [ ] 黎曼度量、Levi-Civita 联络、协变导数
- [ ] 曲率张量、测地线
- [ ] de Rham 上同调：$H^k_{dR}(M) = \ker d / \operatorname{im} d$

### 1.3 关键洞察（de Rham 上同调为什么重要）

回到你的水缸：闭合 1-形式但不是恰当 1-形式的存在性 = 空间的拓扑非平凡性。

$$\oint_C \vec{v} \cdot d\vec{r} \neq 0 \quad \text{但} \quad \nabla \times \vec{v} = 0$$

这在水缸（单连通）里不可能，但在环形容器（有把手）里可能——环量非零但没有涡量。上同调群 $H^1$ 恰好测量"有多少个这样的洞"。

### 1.4 检验标准

- 能独立证明 Stokes 定理（在带边流形上）
- 能计算环面 $T^2$ 的 de Rham 上同调 $H^0, H^1, H^2$
- 能写出球面 $S^2$ 上的体积形式并证明它不是恰当形式

## 阶段二：李群与李代数系统学（2 个月）★核心

你已经理解了 $SO(3)$、$\mathfrak{so}(3)$、指数映射、伴随作用、辛群。现在补全表示论和半单李代数分类。

### 2.1 主线教材（二选一）

| 书 | 风格 |
|---|---|
| 《Lie Groups, Lie Algebras, and Representations》(Brian Hall) | 对物理背景友好，矩阵李群入手，有 SU(2)/SO(3) 表示论的完整处理 |
| 《Naive Lie Theory》(John Stillwell) | 更薄更快，适合先扫一遍 |

### 2.2 必须掌握的内容清单

- [ ] 经典群全景：GL, SL, O, SO, U, SU, Sp，各自的李代数
- [ ] 指数映射的严格性质（满射性、单射性何时成立）
- [ ] 伴随表示 $\text{Ad}$, $\text{ad}$，Killing 型
- [ ] $\mathfrak{su}(2)$ 的表示论（自旋的数学本质！）
- [ ] 根系、Dynkin 图、半单李代数分类（$A_n, B_n, C_n, D_n$ 和例外群）
- [ ] 紧致群的 Peter-Weyl 定理（知道结论即可）

### 2.3 关键洞察（为什么表示论是重点）

量子力学的粒子分类 = 李群表示的分类。

- 电子自旋 1/2 = $SU(2)$ 的 2 维不可约表示
- 光子偏振 = $SO(3)$（或洛伦兹群）的向量表示
- 夸克的味对称 = $SU(3)$ 的表示

粒子物理标准模型的整个结构 = $SU(3) \times SU(2) \times U(1)$ 的表示论。你学会根系和 Dynkin 图之后，回头看基本粒子表会突然"看懂"。

### 2.4 检验标准

- 能推导 $SU(2)$ 与 $SO(3)$ 的双覆盖关系，解释为什么自旋 1/2 转 $4\pi$ 才复原
- 能画出 $A_2$（$\mathfrak{su}(3)$）的根系图和权图
- 能证明 $\mathfrak{sp}(2n)$ 保持辛形式 $\omega$ 不变（你已有直觉，现在要严格写）

## 阶段三：分析力学的几何化（3 个月）★核心

这是你明确点名的"哈密顿力学、欧拉-拉格朗日方程"，也是把前两个阶段汇合的地方。

### 3.1 主线教材

**Arnold《Mathematical Methods of Classical Mechanics》——这本书就是你的圣经，没有之一。**

它恰好把你已经接触的所有东西串成一条线：辛几何、李群、哈密顿系统、可积性。

辅助阅读：José & Saletan《Classical Dynamics》（更现代，纤维丛视角）。

### 3.2 必须掌握的内容清单（按 Arnold 章节）

- [ ] 拉格朗日力学：最小作用量原理 → Euler-Lagrange 方程的变分推导
- [ ] 勒让德变换：$TQ \to T^*Q$，拉格朗日 → 哈密顿
- [ ] 哈密顿方程 = 辛流形上的流
- [ ] 辛几何：$\omega$ 是闭的非退化 2-形式，Darboux 定理
- [ ] 泊松括号 = 李代数结构，$\{f, H\}$ 生成时间演化
- [ ] 诺特定理：对称性 ↔ 守恒量（本书最重要的一节）
- [ ] 刚体 = $SO(3)$ 上的哈密顿力学，欧拉方程
- [ ] 可积系统：Liouville-Arnold 定理、作用-角变量
- [ ] 哈密顿微扰理论入门

### 3.3 关键洞察（诺特定理——整条路线的枢纽）

$$\text{连续对称性（李群作用）} \longleftrightarrow \text{守恒量（泊松对合）}$$

- 时间平移对称（$\mathbb{R}$）→ 能量守恒
- 空间平移对称（$\mathbb{R}^3$）→ 动量守恒
- 旋转对称（$SO(3)$）→ 角动量守恒

你在水缸中读出的每个群，都对应一个守恒量。涡量守恒、环量守恒（Kelvin 定理）都是诺特定理在流体中的体现。

诺特定理的证明只需要：李导数 + 微分形式 + 辛几何——全是你已有的工具。

### 3.4 检验标准

- 从最小作用量原理独立推导 Euler-Lagrange 方程
- 证明诺特定理（用动量映射的语言）
- 用哈密顿形式重推刚体欧拉方程，指出它是 $\mathfrak{so}(3)^*$ 上的李-泊松系统
- 对谐振子做完整的作用-角变量分析

## 阶段四：场论——电磁与规范理论（3 个月）

这是你点名的"电磁蕴含的对称性"。

### 4.1 主线教材

- 入门：《Gauge Fields, Knots and Gravity》(Baez & Muniain) —— 前半部分讲微分形式和 Maxwell，后半讲规范场和纤维丛，风格与你的学习方式完美契合
- 进阶：Nakahara《Geometry, Topology and Physics》

### 4.2 必须掌握的内容清单

- [ ] Maxwell 方程的微分形式表述（你已会：$dF=0,\ d{\star}F = J$）
- [ ] $U(1)$ 规范对称性：$A \to A + d\lambda$，为什么势比场更基本
- [ ] Aharonov-Bohm 效应：规范势的物理实在性 = de Rham 上同调的体现
- [ ] 纤维丛：主丛、联络 1-形式、曲率 2-形式
- [ ] 非阿贝尔规范场：$SU(2)$, $SU(3)$ 杨-米尔斯理论
- [ ] Chern 类、示性类（拓扑量子化的起源）
- [ ] 磁单极 = $U(1)$ 主丛的非平凡拓扑（Dirac 量子化条件 $eg = n\hbar/2$）

### 4.3 关键洞察（规范对称性的几何本质）

电磁势 $A$ 是 $U(1)$ 主丛上的联络，场强 $F = dA$ 是曲率。

规范变换 = 丛的局部截面的选择。Aharonov-Bohm 效应中，电子波函数积累的相位

$$\exp\left(\frac{ie}{\hbar}\oint A\right)$$

是和乐（holonomy）——联络沿闭合曲线的平行移动。磁场为零但相位不为零，因为丛的拓扑非平凡。

你学的每一个数学工具在这里都用上了：微分形式（$A, F$）、李群（$U(1)$）、上同调（AB 效应）、纤维丛（整体结构）。

### 4.4 检验标准

- 用微分形式独立推导电磁波方程，讨论洛伦兹协变性
- 计算 Aharonov-Bohm 相位并解释其拓扑含义
- 写出杨-米尔斯作用量 $\int \text{tr}(F \wedge {\star}F)$，解释每一项的几何含义
- 推导 Dirac 磁单极的量子化条件

## 阶段五：进阶分支（按兴趣选 1-2 个方向）

到这里你已经有了完整的"几何物理"基础。下面按你的兴趣分支：

- **分支 A：几何力学与无穷维系统（回到你的水）**
  - Marsden & Ratiu《Introduction to Mechanics and Symmetry》
  - Arnold & Khesin《Topological Methods in Hydrodynamics》—— 把流体动力学完全几何化：Euler 方程 = $\text{SDiff}(M)$ 上的测地线方程，螺旋度 = 渐近链接数
  - 内容：动量映射、约化理论（Marsden-Weinstein）、KdV 方程与可积系统、涡丝的辫群动力学
  - 这是你水缸问题的"终极答案"
- **分支 B：广义相对论（时空几何）**
  - 《Spacetime and Geometry》(Sean Carroll) 入门 → Wald《General Relativity》进阶
  - 内容：洛伦兹流形、Einstein 方程、黑洞、引力波
  - 与已有知识的连接：测地线 = 变分原理，Einstein-Hilbert 作用量，微分同胚不变性 = 规范对称性
- **分支 C：量子力学的几何化**
  - 《Geometry of Quantum States》(Bengtsson & Życzkowski) 或 Takhtajan
  - 内容：射影希尔伯特空间 = Kähler 流形，Berry 相位 = 和乐，量子力学 = 无穷维辛几何
  - 与李群的连接：$SU(2)$ 表示论 → 自旋，$SO(3)$ 的覆盖群之谜
- **分支 D：泛函分析（你点名了它）**
  - 《Functional Analysis》(Peter Lax) 或 Reed & Simon Vol.1
  - 重点（面向力学）：Hilbert 空间、自伴算子谱理论、无界算子、Sobolev 空间（PDE 弱解的语言）
  - 与哈密顿力学的连接：量子化 = 泊松代数 → 算子代数，$H$ 生成酉群 $e^{-iHt/\hbar}$（Stone 定理——又是李群！）

## 学习方法：怎么"夯实"而不是"看过"

### 1. 费曼式输出（最重要）

每学完一个概念，写一篇 500-1500 字的解释，要求：
- 用水缸作为贯穿例子（流体是你的直觉锚点）
- 必须包含一个自己算过的具体例子
- 必须说明"它统一了什么"

例如学完诺特定理，写一篇《为什么水缸里的漩涡强度不会凭空消失》。

### 2. 计算驱动

不要只读证明，要算。每个阶段至少完成：
- 阶段一：算 $T^2$ 和 $\mathbb{RP}^2$ 的上同调
- 阶段二：手算 $\mathfrak{su}(2)$ 和 $\mathfrak{sl}(2,\mathbb{R})$ 的全部结构常数、验证它们同构（复化后）
- 阶段三：完整解 Kepler 问题（用作用-角变量）
- 阶段四：从 $F$ 出发手推电磁波、辐射规范、Coulomb 规范的区别

### 3. 代码验证

用本项目（graphics/C++，而非现成库）或 Python 画图做：
- 数值模拟 N 点涡系统，验证哈密顿量和角动量守恒
- 画 $SO(3)$ 指数映射的图像
- 用辛积分器（leapfrog）模拟哈密顿系统，对比欧拉法的能量漂移——亲眼看到"辛结构"的价值
- 数值求解二维不可压 Euler 方程（涡量形式），观察涡旋合并

### 4. 交叉复述

学完阶段三后，回到阶段一的教材重读一遍——你会发现很多当时不懂的地方突然通了。知识网络的巩固靠的就是这种回路。

## 总时间表（按每周 10-15 小时投入）

| 月份 | 内容 | 里程碑 |
|---|---|---|
| 1-2 | 阶段零（地基）+ 阶段一启动 | Lee 前 5 章 |
| 3-4 | 阶段一（微分几何） | 证出 Stokes，算出 $T^2$ 上同调 |
| 5-6 | 阶段二（李群） | Hall 全书，画出根系图 |
| 7-9 | 阶段三（Arnold）★ | 证出诺特定理，解完 Kepler |
| 10-12 | 阶段四（规范场） | 算出 AB 相位，写出 YM 作用量 |
| 13+ | 阶段五（选分支） | Arnold-Khesin（分支 A）推荐 |

**优先级建议**：阶段三（Arnold）是你当前知识结构的最大缺口，也是回报最高的部分——它把你已有的李群、辛几何、微分形式全部激活并焊接在一起。如果时间有限，直接冲 Arnold，遇到缺的地基再回头补。

---

## 一句话收束

你的学习路线就是一条"几何化物理"之路——用 Lee 夯实流形与形式的语言，用 Hall 补全李群的表示论，用 Arnold 把力学写成辛几何上的流（诺特定理是枢纽），用 Baez/Nakahara 把电磁升级为纤维丛上的规范理论，最后按兴趣选分支：回到你的水（几何流体力学），或走向时空（广义相对论）、量子（几何量子化）。

检验你是否"夯实"的唯一标准：**能否用微分形式和李群的语言，把水缸中每一个你观察到的现象——波、涡、守恒律——重新讲一遍，且每一步都有严格计算支撑。**
